using System;
namespace For_Challenges
{
    public class Shufflename
    {
        //Create a function that accepts a string of someone's name
        // and returns a string with the names swipped
        public static void Nameshuffle()
        {
            string fullname = "Emmanuel Chimdiebube";
            char space = ' ';
            int num = Convert.ToInt32(space);
            string let = Convert.ToString(space);
            Console.WriteLine(let);
            for (int i = 0; i < fullname.Length; i++)
            {
                if (fullname[i].ToString() == " ")
                {
                    
                }
            }
        }
    }
}